# SocraTeX — Agent Rules

All rules are defined in CLAUDE.md. This file exists for agents that read AGENTS.md instead of CLAUDE.md (e.g., Codex).

See CLAUDE.md for the complete rule set: Socratic method, LaTeX, subject detection, session file rendering, and all skill definitions.
